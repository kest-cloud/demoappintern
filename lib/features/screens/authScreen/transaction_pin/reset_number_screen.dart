import 'package:flutter/material.dart';

class ResetNumberScreen extends StatelessWidget {
  const ResetNumberScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
